FROM ghcr.io/crazy-max/diun:latest

# Add Docker CLI + Compose plugin so script notifications can
# run `docker compose ...` through the mounted docker.sock.
RUN apk add --no-cache docker-cli docker-cli-compose
